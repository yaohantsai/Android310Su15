<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uw.android310.homework1" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- Default activity of the app -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- TODO: Set the default search activity (the one that handles search requests) to be SearchableActivity. -->
            <!-- Your code here -->

        </activity>

        <!-- Provides search suggestions for words and their definitions. -->
        <provider
            android:name=".DictionaryProvider"
            android:authorities="com.uw.android310.homework1.DictionaryProvider" />

        <!-- TODO: Set the launch mode. -->
        <activity
            android:name=".SearchableActivity"
            android:label="@string/title_activity_searchable" >

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.uw.android310.homework1.MainActivity" />

            <!-- TODO: Add an intent filter to receive search requests. -->
            <!-- Your code here -->

            <!-- TODO: Point to searchable configuration. -->
            <!-- Your code here -->
        </activity>
    </application>

</manifest>
